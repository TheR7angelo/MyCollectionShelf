<Page x:Class="MyCollectionShelf.Ui.Book.Pages.AddEditBook"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:MyCollectionShelf.Ui.Book"
      xmlns:page="clr-namespace:MyCollectionShelf.Ui.Book.Pages"
      mc:Ignorable="d"
      Title="Page1"
      DataContext="{Binding RelativeSource={RelativeSource Self}}">
    
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" HorizontalAlignment="Left" AcceptsReturn="False" AcceptsTab="True"
                 Text="{Binding BookTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                 RelativeSource={RelativeSource AncestorType={x:Type page:AddEditBook}}}" />

        <ListBox Grid.Row="1"></ListBox>
        
    </Grid>
    
</Page>
